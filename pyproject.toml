[project]
authors = [{name = "XXXX", email = "XXXX@XXX.com"}]
dependencies = []
name = "hyper-simulation"
requires-python = ">= 3.10"
version = "0.1.0"

[project.scripts]
hypermatch = "hyper_simulation.question_answer.hypermatch:main"

[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

[tool.pixi.project]
channels = ["conda-forge"]
platforms = ["linux-64"]

[tool.pixi.pypi-dependencies]
hyper_simulation = { path = ".", editable = true }

[tool.pixi.tasks]
hypermatch = "python -m hyper_simulation.question_answer.hypermatch"
